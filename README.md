# React-app for memorizing foreign words

### Scripts:

#### `npm run start`
Runs the app in development mode.

#### `npm run build`
Builds the app. The build artifacts will be stored in the `build` directory.
